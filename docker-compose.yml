services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: akenga
      POSTGRES_PASSWORD: akenga_local
      POSTGRES_DB: akenga
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
